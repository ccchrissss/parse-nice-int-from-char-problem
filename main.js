function getAge(inputString){
  
  return Number(inputString[0])

}

console.log(getAge('7 years old'))
  
  // params: inputString  // a phrase where the first character in the string is the age as a number
  // return: age  // an integer
  // e.g. getAge('7 years old')
  // results in 7
  // 

  // return Number(inputString[0])
